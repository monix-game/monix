[build]
	command = "npm run build"
	publish = "dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
# do not force the redirect — allow Netlify to serve existing static files (assets) first
